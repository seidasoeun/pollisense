spring:
  application:
    name: pollisense-simulator

pollisense:
  backend-url: ${BACKEND_URL:http://localhost:8080}
  ingestion-token: ${INGESTION_TOKEN:local-demo-token}
  interval-ms: ${SIMULATOR_INTERVAL_MS:5000}
